[project]
name = "ecoquest-ai-engine"
version = "0.1.0"
requires-python = ">=3.12"

[tool.vercel]
entrypoint = "app:app"

[tool.vercel.functions]
"app.py" = { excludeFiles = "{tests/**,__pycache__/**,**/*.pyc,pytest.ini,conftest.py}" }
