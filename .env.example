# Google Gemma 4 API key - used by the frontend for generative lessons, topics,
# and the Eco Assistant.
# Copy this file to a local .env.local and set your own key. NEVER commit a
# real key. Vite exposes VITE_-prefixed variables via import.meta.env.
VITE_GEMINI_API_KEY=your_gemini_api_key_here